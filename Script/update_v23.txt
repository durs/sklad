CREATE TABLE CONFIG (
    CFG_KEY    TYPE_STR50 NOT NULL,
    CFG_VALUE  TYPE_STR200
);

ALTER TABLE CONFIG ADD CONSTRAINT PK_CONFIG PRIMARY KEY (CFG_KEY);

COMMIT WORK;
EXECUTE PROCEDURE GET_VERSION(1);
COMMIT WORK;